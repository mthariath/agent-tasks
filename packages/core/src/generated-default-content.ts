// This file is generated by scripts/generate-default-content.mjs.
// Do not edit it directly; edit the source files in skills/default-workflow or templates/default instead.

// Source: templates/default/.agent-tasks/README.md
export const GENERATED_DEFAULT_TASKS_README = "# Agent Tasks\n\nThis starter initializes a git-native planning directory.\n\n- `project.yaml` configures statuses and transitions.\n- `WORKFLOW.md` defines the project-local execution contract for agents.\n- `epics/` contains epic files.\n- `tickets/` contains ticket files.\n- `templates/` contains project-local body templates.";

// Source: templates/default/.agent-tasks/WORKFLOW.md
export const GENERATED_DEFAULT_WORKFLOW_GUIDE = "# Agent Workflow\n\nThis project uses `.agent-tasks/` as canonical planning state for humans and agents.\n\n## Agent Contract\n\n- Read `.agent-tasks/project.yaml` before mutating ticket or epic status.\n- Read the relevant ticket and linked epic before starting significant work.\n- Read any referenced supporting docs when they are relevant to the task, especially files such as `PLANS.md`, `DESIGN.md`, specs, or runbooks.\n- Before taking ownership of a ticket, confirm whether it is actually ready from `depends_on` plus workflow `end` semantics.\n- Prefer tickets that are ready now over tickets that are still dependency-blocked.\n- When several tickets are ready, prefer work that can proceed in parallel without colliding with another active owner.\n- If this project uses `assigned_to`, claim a ticket only when actually taking ownership of the work.\n- Keep ticket bodies and frontmatter factual and current while work is underway.\n- Use configured workflow semantics instead of assuming status names such as `blocked` or `done` always exist.\n- If a ticket is too vague to execute safely, refine it before implementation or stop and report why.\n\n## Default Semantics\n\n- `assigned_to`: use a stable human or agent identity such as `mickey`, `codex/main`, or `pi/refiner`.\n- `workflow.start` identifies the primary active work state.\n- `workflow.end` identifies statuses that satisfy dependencies for planning and readiness.\n- `depends_on` lists true prerequisites only; do not create fake serial chains when work can proceed in parallel.\n- Dependency-not-ready and actively blocked are different states: unresolved prerequisites belong in `depends_on`, while a blocked-like workflow state is for started work that is materially stalled.\n- Active work should move into the project's configured active state when one exists and the transition is allowed.\n- If a blocked-like state exists and is reachable, use it when work is materially blocked; otherwise record the blocker in the body without inventing a status.\n- Before marking work complete, make sure acceptance criteria or equivalent body sections still match reality.\n- Keep long-form implementation and design context in referenced supporting docs instead of bloating the ticket body.\n\n## Planner and MCP\n\n- Planning views and commands are advisory helpers over the same files; they do not replace reading the ticket itself.\n- If a local `agenttasks mcp` server is available, agents may use it for structured planning queries or safe mutations, but direct file workflow remains primary.\n\n## Coordinator and Extensions\n\n- If execution behavior or repo-specific automation matters, read `.agent-tasks/extensions/README.md`.\n- `agenttasks serve` is the local coordinator for execution behavior such as claims, start/finish hooks, and project-local commands.\n- Project-local extensions may enforce `before_*` checks or run `after_*` automation around execution lifecycle actions.\n- Treat extensions as policy and automation over the same workflow, not as a second source of truth.\n- If coordinator-backed execution is available, prefer it over ad hoc status or file mutations that would bypass hook-enforced behavior.\n\n## Supporting Docs\n\n- Use ticket or epic `references` for repo-relative supporting docs such as `docs/DESIGN.md`, `PLANS.md`, specs, or runbooks.\n- Treat referenced docs as supporting context, not as a second source of workflow truth.\n- Keep `.agent-tasks/` canonical for status, dependencies, assignment, and execution state.\n\n## Body Conventions\n\nUnless this project adopts another structure, tickets should generally use:\n\n- `Context`\n- `Outcome`\n- `Acceptance Criteria`\n- `Notes`\n\nEpics should generally describe:\n\n- goal\n- scope\n- important notes, risks, or dependencies";

// Source: templates/default/AGENTS.block.md
export const GENERATED_DEFAULT_AGENT_GUIDANCE_BLOCK = "<!-- agenttasks:managed:start -->\n## agent-tasks\n\nThis repository uses `.agent-tasks/` as canonical planning state for project work.\n\n- Before significant implementation, read the relevant ticket or epic plus `.agent-tasks/project.yaml` and `.agent-tasks/WORKFLOW.md`.\n- Read referenced supporting docs such as `PLANS.md`, `DESIGN.md`, specs, or runbooks when they are relevant to the task.\n- Prefer tickets that are actually ready from dependency and workflow semantics.\n- If execution policy or repo-local automation matters, check `.agent-tasks/extensions/README.md`.\n- Keep dependencies and status accurate as work changes.\n- When working under a ticket, keep its status and body current.\n- If coordinator-backed execution is available, prefer lifecycle actions over ad hoc status or file edits.\n- Respect configured workflow transitions instead of assuming status names.\n- Use the installed `agent-tasks` workflow skills when they match the task.\n<!-- agenttasks:managed:end -->";

// Source: templates/default/gitignore.block.txt
export const GENERATED_DEFAULT_GITIGNORE_BLOCK = "# agent-tasks runtime\n.agent-tasks/.runtime/";

// Source: templates/default/.agent-tasks/templates/ticket.md
export const GENERATED_DEFAULT_TICKET_TEMPLATE = "## Context\n\nDescribe the background and constraints for this ticket.\n\n## Outcome\n\nDescribe the concrete result that should exist when this ticket is complete.\n\n## Acceptance Criteria\n\n- Fill in the acceptance criteria for this ticket.\n\n## Notes\n\nOptional implementation notes, links, or follow-up ideas.";

// Source: templates/default/.agent-tasks/templates/epic.md
export const GENERATED_DEFAULT_EPIC_TEMPLATE = "## Goal\n\nDescribe the outcome this epic is meant to achieve.\n\n## Scope\n\nList what belongs inside this epic.\n\n## Notes\n\nOptional planning notes, risks, or dependencies.";

// Source: templates/default/.agent-tasks/extensions/README.md
export const GENERATED_DEFAULT_EXTENSIONS_README = "# Extensions\n\nProject-local coordinator extensions live here.\n\n## How They Work\n\n- `agenttasks serve` and other execution entrypoints load TypeScript extensions from this directory.\n- Extensions can register lifecycle hooks for execution events and explicit project-local commands.\n- Core execution invariants still live in the coordinator. Extensions add policy and automation on top.\n\n## Active Extension Files\n\nThe loader activates:\n\n- `*.ts` files directly in this directory\n- `*/index.ts` files in subdirectories\n\nThe loader ignores:\n\n- `api.ts`\n- `*.example.ts`\n\n## Starting Point\n\n1. Copy an example into this directory or a subdirectory.\n2. Edit the extension id and hook logic.\n3. Start `agenttasks serve` or run a lifecycle command.\n4. Inspect hook runs with `agenttasks hook-runs`.\n\nKeep extension logic project-local and focused. If an extension needs to enforce a rule, prefer a blocking `before_*` hook. If it only reacts to committed work, use an `after_*` hook.";

// Source: templates/default/.agent-tasks/extensions/api.ts
export const GENERATED_DEFAULT_EXTENSIONS_API = "export type ExtensionEventName =\n  | \"after_claim\"\n  | \"before_start\"\n  | \"after_start\"\n  | \"before_finish\"\n  | \"after_finish\"\n  | \"after_block\"\n  | \"after_release\"\n  | \"review_requested\";\n\nexport interface HookBlockResult {\n  block: {\n    detail: string;\n    code?: string;\n  };\n}\n\nexport interface ExtensionLogger {\n  info(message: string): void;\n  warn(message: string): void;\n  error(message: string): void;\n}\n\nexport interface ExtensionHookContext<TPayload extends Record<string, unknown> = Record<string, unknown>> {\n  rootDir: string;\n  event: ExtensionEventName;\n  payload: TPayload;\n  log: ExtensionLogger;\n  getIndex(): Promise<unknown>;\n  block(detail: string, code?: string): HookBlockResult;\n}\n\nexport interface ExtensionCommandContext {\n  rootDir: string;\n  input: Record<string, unknown>;\n  log: ExtensionLogger;\n  getIndex(): Promise<unknown>;\n}\n\nexport interface ExtensionDefinition {\n  id: string;\n  setup(api: ExtensionApi): void | Promise<void>;\n}\n\nexport interface ExtensionApi {\n  on(event: ExtensionEventName, handler: (context: ExtensionHookContext) => void | HookBlockResult | Promise<void | HookBlockResult>): void;\n  registerCommand(name: string, handler: (context: ExtensionCommandContext) => unknown | Promise<unknown>, description?: string): void;\n}\n\nexport function defineExtension<T extends ExtensionDefinition>(definition: T): T {\n  return definition;\n}";

// Source: templates/default/.agent-tasks/extensions/examples/review-queue.ts.example
export const GENERATED_DEFAULT_EXTENSION_REVIEW_QUEUE = "import { defineExtension } from \"../api\";\n\nexport default defineExtension({\n  id: \"review-queue\",\n  setup(api) {\n    api.on(\"review_requested\", async ({ payload, log }) => {\n      log.info(`enqueue ${String(payload.ticketId)} for review in ${String(payload.reviewStatus)}`);\n    });\n  }\n});";

// Source: templates/default/.agent-tasks/extensions/examples/auto-test-on-finish.ts.example
export const GENERATED_DEFAULT_EXTENSION_AUTO_TEST_ON_FINISH = "import { execFile } from \"node:child_process\";\nimport { promisify } from \"node:util\";\nimport { defineExtension } from \"../api\";\n\nconst exec = promisify(execFile);\n\nexport default defineExtension({\n  id: \"auto-test-on-finish\",\n  setup(api) {\n    api.on(\"after_finish\", async ({ rootDir, payload, log }) => {\n      log.info(`running test suite for ${String(payload.ticketId)}`);\n      await exec(\"pnpm\", [\"test\"], { cwd: rootDir });\n    });\n  }\n});";

// Source: templates/default/.agent-tasks/extensions/examples/merge-handoff.ts.example
export const GENERATED_DEFAULT_EXTENSION_MERGE_HANDOFF = "import { defineExtension } from \"../api\";\n\nexport default defineExtension({\n  id: \"merge-handoff\",\n  setup(api) {\n    api.registerCommand(\"prepare-merge-handoff\", async ({ input, log }) => {\n      log.info(`prepare merge handoff for ${String(input.ticketId ?? \"unknown-ticket\")}`);\n      return {\n        ok: true,\n        note: \"fill in your merge or PR handoff behavior here\"\n      };\n    }, \"Prepare a merge handoff payload for a finished ticket.\");\n  }\n});";

// Source: templates/default/.agent-tasks/extensions/examples/stale-worker.ts.example
export const GENERATED_DEFAULT_EXTENSION_STALE_WORKER = "import { defineExtension } from \"../api\";\n\nexport default defineExtension({\n  id: \"stale-worker-escalation\",\n  setup(api) {\n    api.registerCommand(\"escalate-stale-worker\", async ({ input, log }) => {\n      log.warn(`stale worker escalation requested for ${String(input.ticketId ?? \"unknown-ticket\")}`);\n      return {\n        ok: true,\n        escalated: true\n      };\n    }, \"Escalate a stale worker or abandoned ticket for manual attention.\");\n  }\n});";

// Source: skills/default-workflow/creating-tickets/SKILL.md
export const GENERATED_DEFAULT_SKILL_CREATING_TICKETS = "---\nname: creating-tickets\ndescription: Create new agent-tasks tickets using the default workflow pack. Use when a user describes new work that should become a ticket in `.agent-tasks/tickets/`.\n---\n\n# Creating Tickets\n\nUse this skill when new work should be captured as a ticket.\n\n## Required Read Order\n\n1. Read `.agent-tasks/project.yaml`.\n2. Read `.agent-tasks/WORKFLOW.md`.\n3. Read nearby ticket templates or example tickets when they exist.\n\n## Workflow\n\n1. Reuse configured statuses; when unspecified, use the earliest non-terminal status that fits the workflow.\n2. Attach the ticket to an existing epic when one clearly fits.\n3. Use only one directional dependency field: `depends_on`.\n4. Add dependencies only for real prerequisites that must complete before the ticket can start.\n5. Avoid fake serial chains when work can proceed in parallel.\n6. If the project uses `assigned_to`, leave it empty unless the creating agent is explicitly taking ownership immediately.\n7. Add `references` when supporting docs such as `PLANS.md`, `DESIGN.md`, specs, or runbooks materially improve execution clarity.\n8. If execution or review behavior depends on repo-local automation, point at the relevant extension docs or runbooks instead of restating that policy in the ticket.\n9. Keep the body concise, factual, and aligned with the project-local workflow conventions so another agent can tell whether the ticket is ready without guessing.\n\n## Validation\n\n- The created ticket should be actionable without guessing the goal.\n- The title should name the concrete outcome, not just the activity.\n- Dependencies should make readiness clearer, not noisier.\n- References should point at real supporting docs, not placeholder files.\n- Repo-wide automation or approval policy should usually live in workflow docs or extensions, not inside every ticket.\n- Do not invent unsupported implementation details.";

// Source: skills/default-workflow/refining-tickets/SKILL.md
export const GENERATED_DEFAULT_SKILL_REFINING_TICKETS = "---\nname: refining-tickets\ndescription: Refine vague agent-tasks tickets into ready-to-work tickets using the default workflow pack. Use when a ticket exists but needs clearer outcome, acceptance criteria, dependencies, or epic placement before implementation starts.\n---\n\n# Refining Tickets\n\nUse this skill to turn a rough ticket into a ready one.\n\n## Required Read Order\n\n1. Read `.agent-tasks/project.yaml`.\n2. Read `.agent-tasks/WORKFLOW.md`.\n3. Read the target ticket.\n4. Read the linked epic if present.\n\n## Workflow\n\n1. Tighten the title so it names the actual outcome.\n2. Fill in missing body sections only where they materially improve execution clarity.\n3. Add dependencies only when another ticket truly must land first.\n4. Remove or avoid fake serial dependencies when work is actually parallelizable.\n5. Make readiness legible so another agent can tell whether the ticket can start now without guessing.\n6. Add or clean up `references` when long-form plans, design docs, specs, or runbooks should be read alongside the ticket.\n7. If a repo-specific start, finish, review, or escalation rule keeps recurring, prefer documenting it in project-local extension policy instead of bloating the ticket body.\n8. Keep diffs small and preserve still-relevant user-authored notes.\n9. Avoid inventing implementation details that are not supported by the project context.\n\n## Validation\n\n- After refinement, the ticket should be specific enough for an agent or human to begin safely.\n- The dependency shape should reflect real prerequisites, not generic caution.\n- Supporting docs should be referenced instead of copied into the ticket body when that keeps the ticket clearer.\n- Repo-specific execution policy should live in workflow docs or extensions, not repeated ticket prose.\n- If the project uses a custom body pattern, preserve it instead of forcing the default one.";

// Source: skills/default-workflow/working-tickets/SKILL.md
export const GENERATED_DEFAULT_SKILL_WORKING_TICKETS = "---\nname: working-tickets\ndescription: Execute work under agent-tasks tickets using the default workflow pack. Use when implementing, debugging, or otherwise carrying out a specific ticket and keeping it current while you work.\n---\n\n# Working Tickets\n\nUse this skill when actually doing the work described by a ticket.\n\n## Required Read Order\n\n1. Read `.agent-tasks/project.yaml`.\n2. Read `.agent-tasks/WORKFLOW.md`.\n3. Read the target ticket.\n4. Read the linked epic if present.\n5. Read any relevant ticket or epic `references`, especially supporting docs such as `PLANS.md`, `DESIGN.md`, specs, or runbooks.\n6. If execution policy or project automation matters, read `.agent-tasks/extensions/README.md`.\n7. If available, use planner outputs or a local `agenttasks mcp` session as optional helpers for readiness and dependency checks.\n\n## Workflow\n\n1. Confirm the ticket is actionable. If it is too vague, refine it first or stop and explain why execution is unsafe.\n2. Check whether the ticket is actually ready from `depends_on` plus workflow `end` semantics before you claim it.\n3. If the ticket is not ready, do not blindly start it. Either refine its dependencies, switch to truly ready work, or explain concretely why it cannot start.\n4. If the project uses `assigned_to` and you are taking ownership, set it to a stable identity string.\n5. Move the ticket into the appropriate active workflow state only if the transition is allowed.\n6. Keep frontmatter and body factual while work is underway.\n7. Prefer referenced supporting docs for long-form plans or design context instead of copying them into the ticket body.\n8. If new prerequisite work is discovered, update `depends_on` only when the other ticket truly must finish first.\n9. If blockers appear after work has started, use the configured blocked-like state only when one exists and is reachable; otherwise document the blocker in the body without inventing a status.\n10. If coordinator-backed execution is available, prefer lifecycle actions such as claim/start/block/finish over raw file edits that would bypass hooks or project-local automation.\n11. If start or finish is blocked by project policy, inspect the coordinator or extension behavior instead of forcing ticket state by hand.\n12. Before completion, make sure the body, acceptance criteria, dependency state, and relevant references still match reality so downstream tickets unblock correctly.\n13. Move to the appropriate review or terminal state only through allowed transitions.\n\n## Guardrails\n\n- Do not claim a ticket just because you inspected it.\n- Prefer ready-now work when choosing among multiple tickets.\n- Do not force invalid transitions.\n- Do not rewrite user intent casually.\n- Do not add derived state such as reverse dependency fields.\n- Treat MCP as optional local tooling, not a separate source of truth.\n- Do not bypass coordinator-enforced execution policy with direct status edits when lifecycle actions are available.";

// Source: skills/default-workflow/managing-status/SKILL.md
export const GENERATED_DEFAULT_SKILL_MANAGING_STATUS = "---\nname: managing-status\ndescription: Manage agent-tasks status changes safely using the default workflow pack. Use when a ticket or epic needs a status mutation that must respect the configured transition graph in `.agent-tasks/project.yaml`.\n---\n\n# Managing Status\n\nUse this skill for status-only mutations or checks.\n\n## Required Read Order\n\n1. Read `.agent-tasks/project.yaml`.\n2. Read `.agent-tasks/WORKFLOW.md` when workflow semantics matter.\n3. Read the current entity before changing it.\n4. If the status change is part of active execution or review policy, check `.agent-tasks/extensions/README.md`.\n\n## Workflow\n\n1. Confirm the current status before changing it.\n2. Apply only allowed transitions.\n3. Make sure the change still makes sense relative to dependency readiness and downstream planning.\n4. If coordinator-backed execution or review policy is available, do not use raw status changes to bypass lifecycle hooks or project-local automation.\n5. If the desired state is not reachable, explain the blocked transition instead of forcing it.\n6. Update `updated_at` through the normal mutation path.\n\n## Guardrails\n\n- Do not assume statuses like `blocked` or `done` exist in every project.\n- Use workflow semantics and the configured transition graph, not hardcoded names.\n- Do not use a blocked-like status as a substitute for unresolved `depends_on` prerequisites.\n- Treat reaching an end status as a claim that downstream dependencies are now satisfied.\n- Do not bypass coordinator-enforced execution behavior with status-only edits when lifecycle actions are available.";

