# Agent Tasks

This starter initializes a git-native planning directory.

- `project.yaml` configures statuses and transitions.
- `WORKFLOW.md` defines the project-local execution contract for agents.
- `epics/` contains epic files.
- `tickets/` contains ticket files.
- `templates/` contains project-local body templates.
