---
id: T-0009
title: Prepare npm beta release packaging
status: in_review
epic: E-0001
assigned_to: codex/main
created_at: 2026-04-10
updated_at: 2026-04-10
---

Context

The repo is ready for a public beta, but the npm package boundary is still janky. The CLI depended on `@agenttasks/core` via `workspace:*`, core was still private, there was no license, and packed tarballs included development-only files. npm also rejected the unscoped `agenttasks` package name because it is too similar to the existing `agent-tasks` package, so the published CLI package had to move to `@agenttasks/cli` while keeping the executable name `agenttasks`.

Outcome

The first public npm release is publishable as a clean beta without packaging surprises.

Acceptance Criteria

- `@agenttasks/cli` and `@agenttasks/core` are both publishable packages.
- Tarballs contain only intentional runtime files.
- The install/runtime contract is explicit, including Bun as a requirement for `agenttasks tui`.
- Release docs and smoke checks cover the beta release path.

Notes

This is a packaging and release-hygiene pass, not a feature pass.

Completed work

- Made `@agenttasks/core` public and publishable alongside `@agenttasks/cli`.
- Added MIT licensing and package metadata for both published packages.
- Restricted published tarballs to runtime files only.
- Added package READMEs and beta install/release docs.
- Added `scripts/smoke-release.sh` and root `pnpm release:smoke`.
- Added a clear Bun-required error path for `agenttasks tui`.
- Renamed the published CLI package to `@agenttasks/cli` while preserving the `agenttasks` executable name.

Verification

- `pnpm build`
- `pnpm typecheck`
- `pnpm test`
- `zsh scripts/smoke-release.sh`

Smoke results

- packed `@agenttasks/core` and `@agenttasks/cli` tarballs successfully
- confirmed tarballs exclude `src/`, `test/`, and `.turbo/`
- installed both tarballs into a clean temp project
- verified `agenttasks --help`, `init`, `validate`, `ready`, and `plan`
- verified `agenttasks tui` fails cleanly with an explicit Bun requirement when Bun is absent

## Execution Handoff

- codex/main 2026-04-10: implementation complete, ready for integration

## Execution Handoff

- codex/main 2026-04-10: implementation complete, ready for integration
